<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Innercise</title>
  </head>
  <body>
    <%= link_to root_path do %>
       <button>Back</button>
    <% end %>
  
  <% if @workouts.empty? %>
    <p> Sorry no videos because quota has been reached </p>
  <% else %>  
    <%= render partial: "videos", locals: { workouts: @workouts, id: @workout_id } %> 
  <% end %>


  </body>
</html>

